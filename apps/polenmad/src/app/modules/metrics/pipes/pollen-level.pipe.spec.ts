import { PollenLevelPipe } from './pollen-level.pipe';

describe('PollenLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new PollenLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
